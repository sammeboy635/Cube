#pragma once
#include "../../incl/GLFW/glfw3.h"

static void callback_error(int error, const char *description);
static void callback_key(GLFWwindow *screen, int key, int scancode, int action, int mods);
